<html>
<head>
    <title>Contacts List</title>
</head>
<body>
    <h1>Your list of contacts. Feel free to add new contacts!</h1>
    {% if contacts is not empty %}
    <p>Here are all of your contacts:</p>
    <ul>
    {% for contact in contacts %}
        <h3>{{ contact.getName }}</h3>
        <li>{{ contact.getPhone }}</li>
        <li>{{ contact.getAddress }}</li>
    {% endfor %}
    </ul>
    {% endif %}

    <form action='/contacts' method='post'>

        <div class='form-group'>
          <label for='name'>Enter your new contact's name:</label>
          <input id='name' name='name' type='text'>
        </div>

        <div class='form-group'>
          <label for='phone'>Enter your new contact's phone number:</label>
          <input id='phone' name='phone' type='number'>
        </div>

        <div class='form-group'>
          <label for='address'>Enter your new contact's address:</label>
          <input id='address' name='address' type='text'>
        </div>


        <button type='submit'>Add Contact!</button>
    </form>

    <form action='/delete_contacts' method='post'>
        <button type='submit'>Clear your contacts list</button>
    </form>

</body>
</html>
